package Submit;

public class SLogin {

    public SLogin(){}
    /*
    public VAccount login(String id, String password){
        EAccount eAccount=new EAccount();
        VAccount vAccount=new VAccount();
        //얘는 entity한테 얘기해야 함
        return eAccount.getLogin(id,password);//eAccount에게 해당하는 id를 가져와 달라 요청함
    }

     */
}
